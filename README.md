# TypeScript Starter Project using esbuild

Set `ESLINT_USE_FLAT_CONFIG=true` to enable [ESLint Flat Config](https://eslint.org/docs/v8.x/use/configure/configuration-files-new) until ESLint is upgraded to 9.0.
